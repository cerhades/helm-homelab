{{- if .Values.service.internal.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "dashy.fullname" . }}-local
  labels:
    {{- include "dashy.labels" . | nindent 4 }}
spec:
  {{- with .Values.service.internal }}
  type: {{ .type }}
  ports:
  - port: {{ .port }}
    targetPort: {{ .targetPort }}
    nodePort: {{ .nodePort }}
    protocol: {{ .protocol }}
    name: {{ .name }}
  {{- end }}
  selector:
    {{- include "dashy.selectorLabels" . | nindent 4 }}
{{- end }}
